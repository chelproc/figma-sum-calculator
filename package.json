{
  "name": "figma-sum-calculator",
  "version": "1.0.0",
  "description": "Calculate the sum of all the numeric strings in the selection",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "npm run build -- --watch"
  },
  "license": "MIT",
  "devDependencies": {
    "@figma/plugin-typings": "^1.52.0",
    "typescript": "^4.8.4"
  }
}
